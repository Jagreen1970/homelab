---
# Ansible collection dependencies
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansibleguy.opnsense
    # OPNsense REST API modules: HAProxy, Unbound DNS, packages, certs, and more
    # Docs: https://github.com/ansibleguy/collection_opnsense
